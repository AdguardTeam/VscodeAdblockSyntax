# @vscode-adblock-syntax/client

Language client for Adblock/AdGuard/uBlock filters syntax.

This package implements the VSCode language client that communicates with the language
server to provide linting and language features for adblock filter lists.
