root: true
platforms:
  - adg_any
extends:
  - aglint:recommended
rules:
  max-css-selectors: [warn, { maxSelectors: 1 }]
  no-excluded-rules:
    - error
    - excludedRegExpPatterns:
        # Do not forget to escape special regex characters
        - example\.com\/bad\/query\/
