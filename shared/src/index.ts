// TODO: Add shared validation logic, schemas (e.g., Valibot), custom LSP protocol types,
// and other utilities that need to be consistent between client and server.

export { FileScheme } from './file-scheme';
