{
    "name": "@vscode-adblock-syntax/shared",
    "description": "Shared utilities for vscode-adblock-syntax packages.",
    "version": "1.0.0",
    "license": "MIT",
    "author": "AdGuard Software Ltd.",
    "private": true,
    "files": [
        "out"
    ],
    "exports": {
        ".": {
            "types": "./out/index.d.ts",
            "import": "./out/index.js"
        }
    },
    "typesVersions": {
        "*": {
            "*": [
                "out/index.d.ts"
            ]
        }
    },
    "engines": {
        "node": ">=20.0.0"
    },
    "scripts": {
        "prebuild": "rimraf out",
        "build": "tsx scripts/build.ts",
        "postbuild": "tsc --project tsconfig.build.json --emitDeclarationOnly",
        "test": "vitest",
        "lint:code": "eslint . --cache --ext .ts --ext .js --ext .cjs",
        "lint:md": "markdownlint .",
        "lint": "pnpm run lint:code && pnpm run lint:md"
    },
    "devDependencies": {
        "esbuild": "catalog:",
        "rimraf": "^6.1.2",
        "tsc-files": "catalog:",
        "typescript": "catalog:",
        "vitest": "catalog:"
    }
}
